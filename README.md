# Time_Clock
Small wpf app to compare Time with stopper function, using custom class Time_TimePeriod

# Description
App uses custom structures Time, TimePeriod which was created to manage time variable and unit testings for logic.

# Main features:
* Compare Time
* Sum and substract different Time values
* Overrides ToString method for easy display
